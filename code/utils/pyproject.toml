[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hyui-utils"
version = "0.0.1"
dependencies = [
]

[project.optional-dependencies]
test = [
  "pytest == 7.1.3"
]

[tool.pytest.ini_options]
minversion = "7.1.3"
testpaths = [
    "src/utils/tests"
]
