# pytest.ini
[pytest]

addopts =
    --strict-markers
    -ra
    -vv
    --doctest-modules

doctest_optionflags = "NORMALIZE_WHITESPACE"

testpaths =
    tests

markers =
    smoke: smoke tests to define base functionality
    unit: unit tests
    e2e: end-2-end tests
