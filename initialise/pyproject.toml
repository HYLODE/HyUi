[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hyui-initialise"
version = "0.0.1"
dependencies = [
    "email-validator == 1.3.0",
    "pydantic == 1.10.2",
    "requests == 2.28.1"
]
